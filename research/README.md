# Research evidence workspace

This directory contains the reproducible claim audit and follow-up experiments
for the F8 and CASI work. It deliberately separates claims, executable
experiments, raw results, and interpretation.

## Layout

- `claims/`: one row per material paper claim, with exact evidence paths.
- `configs/`: immutable experiment parameters.
- `experiments/`: executable research code; no paper prose is generated here.
- `results/`: raw machine-readable outputs and run manifests.
- `reproduction/`: frozen inventory, code-version protocol, and legacy runner.
- `reports/`: reviewed interpretation of the raw outputs.
- `ATTEMPT_LOG.md`: every attempted experiment, including negative or failed runs.

## Reproduction

From the repository root:

```bash
./scripts/reproduce_papers.sh
./scripts/reproduce_research.sh
```

The PQC track has a separate real-backend runner:

```bash
./scripts/reproduce_pqc.sh
```

It pins `pqcrypto==0.4.0`, functionally gates every KEM/signature operation,
then runs the output controls, the ML-KEM NTT trace, and the ML-KEM packing
isolation. It also runs a balanced BvN-route causal gate: global bijections
preserve every 32-byte output row exactly while changing only row order. This
separates output-order/serialization effects from claims about internal PQC
state. Its output hash manifest is `results/v1/PQC_SHA256SUMS`.

The route construction and its calibration are documented in
`reports/BVN_ROUTE_CAUSAL_V1.md`. Use it for any new sequential PQC statistic
before interpreting an elevated CASI or F8-O score.

The operation-level causal track has its own runner:

```bash
./scripts/reproduce_causal_mechanisms.sh
```

It validates the exact cryptographic `.causal` builder/reader, runs paper-scale
carry interventions over all F8 targets, checks the corrected ML-KEM
`Uniform(Z_q) -> Compress_d` cause, evaluates six lossless compressor families
and all ordered depth-two cascades, and computes the conditional code-length
form of F8. The reviewed interpretation is
`reports/CAUSAL_MECHANISM_RESULTS_V1.md`; all retained artifacts are pinned in
`results/v1/CAUSAL_SHA256SUMS`.

The cross-cipher output atlas is reproduced separately:

```bash
./scripts/reproduce_multi_cipher_atlas.sh
```

It overlays null-corrected 32-byte information heatmaps for ChaCha and AES
round prefixes, SIMON, Threefish, random bytes, ML-KEM, ML-DSA and HQC. Whole-
operation controls prevent PQC serialization from being mislabeled as
inter-operation dependence. The focused 10-seed frontier confirmation and its
interpretation are in `reports/MULTI_CIPHER_CAUSAL_ATLAS_V1.md`; hashes are in
`results/v1/ATLAS_SHA256SUMS`.

The open PQC representation track is reviewed in
`reports/OPEN_PQC_INFERENCE_MLKEM_V1.md`.  Its ML-KEM baseline uses real
Native/PQClean `poly_decompress -> NTT` calls and must be interpreted with the
same-type random-bit control; the public NTT locality effect is not a security
claim.

The next causal boundary composition, selected from the reader graph, is
documented in `reports/OPEN_PQC_COMPRESSION_BOUNDARY_V1.md`.  It combines the
decompression/NTT breadcrumb with the unequal-preimage `Compress_d` breadcrumb
and closes the fixed-coordinate line under a same-weight random-position null.

The active planning queue is generated by
`experiments/causal_breadcrumb_miner.py`; it reads every `.causal` file with
the reader, removes generic metric-token collisions, deduplicates bridges, and
marks candidates as hypotheses rather than silently promoting them to causal
facts.

The direct cipheroutput-to-causal-file track is documented in
`reports/DIRECT_OUTPUT_CAUSAL_AES_R3_V1.md`.  Its confirmed AES result uses
the actual factual-pair versus row-repair intervention contrast as the causal
payload and performs its holdout reverse query by reopening the `.causal` file.
The shared-route/pooled full AES prefix frontier is documented in
`reports/DIRECT_OUTPUT_CAUSAL_AES_FRONTIER_V1.md`.
The asymmetric input-domain transfer boundary is documented in
`reports/DIRECT_OUTPUT_CAUSAL_AES_DOMAIN_BOUNDARY_V1.md`.
The independent ChaCha transfer is documented in
`reports/DIRECT_OUTPUT_CAUSAL_CHACHA_R3_V1.md`.
The hardened R2 core graph and its asymmetric counter-domain boundary are
documented in `reports/DIRECT_OUTPUT_CAUSAL_CHACHA_R2_DOMAIN_V1.md`.
The shared-BvN-route correction, output-view ablations and retained binary
core bit-transfer are documented in
`reports/DIRECT_OUTPUT_CAUSAL_ROUTE_CONTROL_V1.md`.
The adjacent-output traversal sweep, including Gray, bit-reversal, affine and
random-order controls, is documented in
`reports/DIRECT_OUTPUT_CAUSAL_CHACHA_TRAVERSAL_V1.md`.
The real deterministic ML-KEM coin-pair null is documented in
`reports/DIRECT_OUTPUT_CAUSAL_MLKEM_COIN_V1.md`.
The native pre-FO INDCPA noise-coin direct-output null is documented in
`reports/DIRECT_OUTPUT_CAUSAL_MLKEM_INDCPA_NOISE_V1.md`.
The orthogonal native INDCPA keyseed-to-public-key null is documented in
`reports/DIRECT_OUTPUT_CAUSAL_MLKEM_KEYSEED_V1.md`.

The sequential ChaCha inference night run, including the retracted subspace
false positive and its structure-preserving matching correction, is reviewed
in `reports/NIGHT_RUN_CHACHA_INFERENCE_V1.md`.  New work must use the fair
fixed-point-free involution null whenever the chosen input difference is a
perfect matching on a closed counter subspace.

The current direct-output nightrun evidence matrix, including retained objects
and closed branches, is `reports/NIGHTRUN_DIRECT_CAUSAL_MATRIX_V1.md`.

The direct Threefish full-round output-causal null is reviewed in
`reports/DIRECT_OUTPUT_CAUSAL_THREEFISH_V1.md`.
The corresponding direct SIMON32/64 late-round null is reviewed in
`reports/DIRECT_OUTPUT_CAUSAL_SIMON_V1.md`.
The KAT-gated direct PRESENT-80 prefix screen is reviewed in
`reports/DIRECT_OUTPUT_CAUSAL_PRESENT80_V1.md`.

The predicted PRESENT-80 to PRESENT-128 full-round F8 transfer, its five-key
BvN confirmation, fixed-point localization, and exact seven-edge population
theorem are reviewed in `reports/FULLROUND_CAUSAL_PRESENT128_V1.md`.  This is
the thirteenth retained full-round configuration; the exact mechanism script
derives the support without rerunning established anchors as a hedge.

The complete transfer track is one command:

```bash
./scripts/reproduce_fullround_transfers.sh
```

It reruns the PRESENT-128-through-SHAKE transfer sequence, including the
A133--A138 symbolic Reader block, opens every causal artifact through the
repository validator, runs the focused tests, and writes
`results/v1/FULLROUND_TRANSFER_SHA256SUMS`.

The SHA-256 full 64-step feed-forward result, its SHA-512 80-step word-width
transfer, random-chaining confirmation, and the exact full-word carry spectrum
are reviewed in `reports/FULLROUND_CAUSAL_SHA2_FEEDFORWARD_V1.md`.

The NTT-gated FEAL-32X R30→R32 full-round relation is reviewed in
`reports/FULLROUND_CAUSAL_FEAL32X_V1.md`.  Its raw single-bit graph identifies
the marginalization boundary; the retained five-edge `.causal` graph carries
an executable inverse that reconstructs the complete 32-bit state half.

The SHACAL-2 R63→R64 shared-accumulator cancellation is reviewed in
`reports/FULLROUND_CAUSAL_SHACAL2_V1.md`.  Its three-edge Reader graph removes
the common `T1` branch by modular subtraction and reconstructs one complete
R63 word exactly.

The SPARKLE-256/384/512 complete-permutation endpoint result is reviewed in
`reports/FULLSTEP_CAUSAL_SPARKLE_V1.md`.  Its 15-edge Reader artifact stores
the exact final-step half-state projection, full-step recipes based on the
linear-layer minimal orders 6/30/12, and complete-basis proofs of the
four-word aggregate quotient orders 3/10/3.

The BLAKE3 seven-round compression output result is reviewed in
`reports/FULLCOMPRESSION_CAUSAL_BLAKE3_V1.md`.  It separates the 32-byte
lane-pair-XOR projection from the complete 64-byte-output-plus-CV Reader and
derives the information-bearing XOR-to-subtraction control as an exact
four-state coupled-borrow spectrum.

The standard ChaCha20 20-round block endpoint is reviewed in
`reports/FULLROUND_CAUSAL_CHACHA20_FEEDFORWARD_V1.md`.  Its public Reader
reconstructs the eight constant/counter/nonce-position core lanes without key
inputs; the known-key Reader reconstructs all sixteen.  A separate causal
recipe predicts the complete 16x32 XOR-peeling carry spectrum from the actual
feed-forward addend words.

The PQC-relevant SHAKE128/256 output boundary is reviewed in
`reports/FULLROUND_CAUSAL_SHAKE_RATE_V1.md`.  Complete first squeeze blocks
reconstruct the post-Keccak-f[1600] rate lanes after all 24 rounds, while
complete basis proofs retain the exact 256-/512-bit capacity kernels.

The next-block capacity observability result is reviewed in
`reports/FULLROUND_CAUSAL_SHAKE_CAPACITY_JACOBIAN_V1.md`.  Ten complete
capacity-to-rate Boolean Jacobians have full capacity rank, and the same-base
Reader identifies every single-bit intervention while cross-base and
two-bit-interaction controls delimit the local nonlinear scope.

The first actual consecutive-block capacity reverse query is reviewed in
`reports/FULLROUND_CAUSAL_SHAKE_CAPACITY_WINDOW_V1.md`.  With the capacity
complement fixed, the Reader uniquely infers 8-, 12-, 16-, and 20-bit windows
for SHAKE128/256 and pins the exact `2^k` enumeration frontier a stronger solver
must beat.

The exact Boolean reconstruction and round-localized observability frontier are
reviewed in `reports/FULLROUND_CAUSAL_SHAKE_SOLVER_FRONTIER_V1.md`. The common
runner is:

```bash
./scripts/reproduce_shake_solver_frontier.sh
```

It reconstructs and proves uniqueness for 4/8/12-coordinate SHAKE128 windows,
records the 16-coordinate Z3 scaling boundary, and evaluates complete `2^16`
truth spaces for SHAKE128/256. Hashes are pinned in
`results/v1/SHAKE_SOLVER_FRONTIER_SHA256SUMS`.

The joint-relation extension is reviewed in
`reports/FULLROUND_CAUSAL_SHAKE_AFFINE_HULL_V1.md`. Under the same declared
known-complement model, exact 128-coordinate GF(2) affine-hull membership
identifies the actual 10-bit prefix in both SHAKE variants. The current
construction still evaluates all `2^16` assignments; it is not an exponent
reduction.

The exact restricted ANF frontier is reviewed in
`reports/FULLROUND_CAUSAL_SHAKE_ALGEBRAIC_DEGREE_V1.md`. Complete fast Möbius
transforms show the first 128 rate-coordinate functions moving from sparse,
balanced ANFs at R3 to full degree 16 and random-like density at R5, remaining
saturated through R24. The stated scope is one complete deterministic 16-bit
window per variant under the known-complement model.

The multi-window Boolean-influence follow-up is reviewed in
`reports/FULLROUND_CAUSAL_SHAKE_BOOLEAN_INFLUENCE_V1.md`. Across three complete
16-bit windows per variant, R3 is already nearly all-to-all in variable support
and measured R4, R5, and R24 are fully coupled and influence-balanced. A133
subsequently distinguishes this coordinate-support frontier from the already
complete R2 monomial interaction graph.

The A133 shared-ANF transform is reviewed in
`reports/FULLROUND_CAUSAL_SHAKE_ANF_COMPRESSION_V1.md`. It stores a shared
monomial dictionary plus packed coordinate matrix, compares raw and transformed
generic codecs and double cascades, and reopens a four-record binary pack to
reconstruct every persisted truth value.

The A134 direct compiler is reviewed in
`reports/SHAKE_SYMBOLIC_R2_ANF_FRONTIER_V1.md`. It derives all 1,600 R2
coordinate polynomials without `2^k` materialization and extends the exact
interface through complete SHAKE128 and SHAKE256 capacities.

The full-round symbolic Reader sequence is reviewed in
`reports/FULLROUND_CAUSAL_SHAKE_SYMBOLIC_R2_SMT_V1.md`,
`reports/FULLROUND_CAUSAL_SHAKE_SYMBOLIC_R2_PARTITION_V1.md`,
`reports/FULLROUND_CAUSAL_SHAKE_SYMBOLIC_SPLIT_FRONTIER_V1.md`, and
`reports/FULLROUND_CAUSAL_SHAKE_SYMBOLIC_R1_SCALING_V1.md`. A135 preserves
native n-ary XOR across the R2 handover; A136 performs the complete
ground-truth-blind width-16 prefix schedule; A137 selects R1 as the measured
minimum-decision interface; and A138 resolves the unpartitioned width-16 model.
The common runner and manifest remain
`scripts/reproduce_shake_solver_frontier.sh` and
`results/v1/SHAKE_SOLVER_FRONTIER_SHA256SUMS`.

Every `.causal` artifact is an exact, typed cryptographic evidence graph, not
an opaque sidecar.  `CryptoCausalReader` checks the CAUSAL header/version,
canonical graph digest, triplets, and inferred-edge provenance.  New AES runs
perform this read-back immediately after writing; the full repository gate is:

```bash
.venv/bin/python scripts/validate_causal_artifacts.py
```

The format stores machine-readable parameters, evidence kind, source,
confidence, attributes, and provenance inside the file.  It deliberately does
not claim to contain unstructured LLM chain-of-thought; the causal content is
the typed, auditable reasoning graph itself.

The second command is fail-closed: it starts follow-up work only after all 39
historical F8 experiments and the Nano master rebuild have completed.

Paper reproduction is performed first with the read-only legacy runner described
in `reproduction/REPRODUCTION_PROTOCOL.md`. Follow-up results under `results/v1/`
are preliminary until that baseline closes. Corrected primitive reruns are kept
separate from historical replay.

## Evidence statuses

- `EVIDENCED`: script, retained raw result, and exact reproduction command exist.
- `PARTIAL`: at least one of those three artifacts is missing or mismatched.
- `MISSING`: no local executable/data evidence was found.
- `CONTRADICTED`: retained evidence directly disagrees with the claim.
- `EXTERNAL_ONLY`: support depends on a cited external primary source.

No security claim is promoted from an experiment merely because a p-value is
small. The relevant cryptographic security game and ideal-world control must be
stated separately.

# A447 personal authentic Causal Reader readback

## Reader gate

I opened
`research/results/v1/chacha20_round20_w46_proof_antecedent_calibration_a447_v1.causal`
directly with `dotcausal.io.CausalReader(..., verify_integrity=True)` from the
anchored reader source `dotcausal/io.py` SHA-256
`e320f77855a713e44c97fbc9d1bbb8c488a5c458f2b5ddecc0254a7dc57e0074`.
The native file reopened as API `a447dag` with three explicit triplets, one
materialized inferred triplet, three embedded rules, one cluster, and one gap.

## Semantic chain personally decoded

1. `A446:assumption_cut_provenance_boundary`
   -- `solver_native_LRAT_antecedent_instrumentation_before_clause_output` -->
   `A447:exact_proof_antecedent_corpus`.
2. `A447:exact_proof_antecedent_corpus`
   -- `typed_DAG_feature_extraction_three_direction_exact_ranks` -->
   `A447:crossfit_proof_rank_panel`.
3. `A447:crossfit_proof_rank_panel`
   -- `eight_complete_block_worst_first_scale_gate` -->
   `A447:proof_antecedent_full128_scale_ready`.
4. The AI-native materialized closure connects the original A446 boundary
   directly to `A447:proof_antecedent_full128_scale_ready` with evidence stage
   `EXACT_PROOF_ANTECEDENT_MECHANISM_FULL128_SCALE_READY`.

## What the graph and result say together

The retained corpus contains 8,192 complete candidate cells across 32 targets
and four horizons: 936,982,340 solver-derived proof nodes and 2,878,295,495
exact antecedent edges, with zero missing parents. The online reduction yields
1,017 base features and 3,051 fixed directional rank fields.

The selected frozen operator is `hybrid_proof_top4_equal`. Its eight held-block
gains are:

`+0.831640, +0.039529, +0.689360, +0.195849, +1.235220, +0.141381, +0.160138, +0.110201` bits.

Thus it is positive in all eight blocks, with minimum `+0.039528804` bits and
balanced gain `+0.425414456` bits. The fixed A442 Borda baseline is positive in
six of eight blocks, with minimum `-0.246890559` bits and balanced gain
`+0.446109429` bits. A447 therefore converts the two-block Borda failure into
an eight-of-eight positive frontier while retaining nearly the same aggregate
gain. Both predeclared scale gates pass.

The mechanistic center is unusually stable. In every one of the eight
block-exclusive training folds, the first two selected rank fields are:

- `h8::all::ancestry_assumption_position_union::position5::ascending`
- `h8::redundant::ancestry_assumption_position_union::position5::ascending`

The Reader is detecting whether candidate assumption position 5 enters the
transitive derivation ancestry by conflict horizon 8. Lower ancestry exposure
is consistently ranked first. The remaining top-four slots vary by held block
and include horizon-2 deletion behavior, horizon-4 parent-size/fanin structure,
other assumption positions, and opposition/event interactions. This identifies
a stable primary path plus complementary proof-topology corrections, not a
single scalar activity magnitude.

No W52 label, W52 refit, W52 solver stage, or candidate execution was used in
A447. The native graph's sole next gap is therefore exactly right:

`complete_128_target_proof_antecedent_corpus`

with the prescribed action to scale the frozen proof Reader across all 128
calibration targets before any target-blind W52 proof trace.

## Bound conclusion

A447 establishes that exact proof-derivation ancestry contains a
cross-block-stable R20 candidate signal that was not available in A446's
clause-output hypergraph. The immediate next experiment is a no-refit transfer:
reuse the 32 measured targets, trace the remaining 96 targets, apply the fixed
A447 top-four hybrid to those 96 unseen targets, and only after that result
construct the complete-128 model for W52.

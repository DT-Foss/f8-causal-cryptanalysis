# A441 personal Causal Reader readback

The authentic `dotcausal.io.CausalReader` reopened
`research/results/v1/chacha20_round20_w52_direct_product_reader_pair_portfolio_a441_v1.causal`
with API id `a441pair`, four explicit triplets, one materialized inferred triplet,
four embedded rules, one cluster, and one gap.

## Retained semantic chain

1. Four frozen prefix Readers crossed with four frozen off-axis Readers produce
   sixteen exact product pair permutations before any axis-first collapse.
2. Stable minimum-rank bucket merging produces one duplicate-free complete
   16,777,216-cell order with zero violations of
   `R_A441 <= 16 * min product-Reader rank`.
3. The exact all-pair comparison shows that direct product preservation and
   A439 axis-first factorization are nearly the same global geometry:
   Spearman `0.9999790521770737`; median rank ratio `1.0`; 99 percent of direct
   ranks lie between `0.9857033` and `1.0144918` of A439.
4. The direct order is earlier on 8,267,108 cells and later on 8,378,344 cells,
   so neither order dominates; the exceptional tail reaches a twofold gain or
   1.958-fold loss.

## Research decision generated by the graph

The embedded gap proposes a factor-two A439/A441 merger. The personal semantic
readback rejects that as the immediate high-leverage experiment because the two
orders are almost perfectly correlated and another recovery allocation would
mostly repeat the same search geometry. A441 instead localizes the missing
degree of freedom: the next schedule must change the aggregation geometry
(coverage/min-rank versus consensus/intersection), not merely preserve the same
Readers one stage longer.

A442 therefore evaluates seven predeclared meta-Reader geometries on the frozen
128-target A375 Known-key panel and transfers the selected geometry to both W52
axes without target labels, Reader refits, or candidate execution.

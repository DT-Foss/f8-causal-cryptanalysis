# Personal Causal Reader readback — A453

The main agent opened `research/results/v1/chacha20_round20_w52_deadline_compiled_proof_portfolio_a453_v1.causal` through `dotcausal.io.CausalReader` with integrity verification enabled and read every explicit triplet, every materialized inferred triplet, both embedded rules, the cluster, and the open gap.

## Native identity

- API ID: `a453dl`
- Explicit triplets: 3
- Materialized inferred triplets: 2
- Embedded rules: 2
- Clusters: 1
- Gaps: 1
- Causal SHA-256: `1e852e39386cc345793434f6dee99f849fb063584ac59aa78c29bfe9668a03e0`

## Semantic chain read from the file

1. A448's three independently constructed proof-Reader rank fields are combined by the latest-free deadline majority-consensus compiler. On the complete128 crossfit panel, the deadline-median operator remains positive on all eight blocks and improves A451's minimum block gain by `+0.012143592043489804` bits.
2. The independent A447-fixed remaining96 transfer reverses that local advantage: A451 retains `0.46459407276558196` aggregate gain and `0.13365659540716646` minimum-block gain, whereas deadline-median records `0.4250321494711695` and `0.04478649598555684`. This identifies global majority consensus as a calibration-sensitive intervention rather than a universal improvement.
3. The exact same frozen median-priority rule is compiled over all `16,777,216` W52 pair cells. The native predecessor-set compiler assigns every cell exactly once and satisfies `compiled_rank <= 3 * best_component_rank` with zero violations.
4. The resulting stream is materially distinct from A451: Spearman `0.9603826101418811`, top-65,536 overlap `0.8830413818359375`, only 13 cells at identical global ranks, and 13,075,914 cells moved earlier relative to A451.
5. The two materialized inference rows close the chain from calibration and target-blind proof orders to the exact all-cell guarantee and from that guarantee to a random-access W52 recovery-ready stream.

## Mechanistic conclusion

A453 proves that arbitrary target-blind consensus priorities can be compiled into a complete non-factorized recovery order without surrendering a hard best-Reader bound. The remaining96 result supplies the decisive routing signal: preserve A451's first-encounter robustness as the primary recovery portfolio, and use deadline compilation for priorities selected on genuinely no-refit transfer rather than complete128 agreement alone.

## Open gap read from the file

The embedded gap requests a qualified recovery execution of the A453 stream. The graph evidence refines that instruction: execution is scientifically valid, but the next design step with higher expected information is a no-refit-selected deadline priority or a constrained hybrid that preserves A451's early union cover while spending only measured duplicate slack on consensus reordering.

# A454 authentic Causal Reader — personal semantic readback

The main agent opened
`research/results/v1/chacha20_round20_w52_no_refit_weighted_reader_portfolio_a454_v1.causal`
directly with `dotcausal.io.CausalReader(verify_integrity=True)` and read every
explicit triplet, materialized inferred triplet, embedded rule, cluster, and gap.

## Reader identity and cover

- API ID: `a454wgt`
- Explicit triplets: 3
- Materialized inferred triplets: 2
- Embedded rules: 2
- Clusters: 1
- Gaps: 1
- File SHA-256: `3c14b8a31484bd6bda279d5010056ee7700af89dd81707244f7fafcdf255063f`

## Explicit semantic chain

1. `A453:remaining96_consensus_boundary`
   --`prospectively_enumerated_periodic_slot_weighting`-->
   `A454:selected_no_refit_weighted_pattern`.

   The prospectively fixed 248-pattern family selected `BOOHH` using only the
   A448 remaining96 A447-fixed-model no-refit scope. Relative to the exact A451
   `BHO` control, aggregate gain increased by `+0.007106867744083978` bit,
   minimum fixed-block gain increased by `+0.02710248522403713` bit, and one
   additional target reached median-or-better rank. All eight blocks remained
   positive.

2. `A454:selected_no_refit_weighted_pattern`
   --`native_periodic_first_encounter_compilation`-->
   `A454:complete_W52_pair_permutation`.

   The selected schedule was materialized as all 16,777,216 W52 pair cells,
   exactly once, in a 67,108,864-byte random-access stream. Its SHA-256 is
   `a82fbe129f6eccaf2ddd560064df1efb471668a116cd52a97490bc66b720b749`.

3. `A454:complete_W52_pair_permutation`
   --`exact_per_component_proposal_bound_validation`-->
   `A454:exact_component_bounded_W52_stream`.

   Every cell satisfies all three component-specific proposal deadlines over
   the complete pair domain. The observed rank ratio to the primary hybrid
   Reader is at most `2.0`, with median `1.0475054458349307` and p99
   `1.1803249388334063`. The ratio to the best component for each cell is at
   most `3.2686567164179103`, median `1.1059190103672694`, and p99
   `1.6174374556843603`.

## Inferred closure

The retained inferred chain closes the A453 remaining96 boundary directly to
the exact bounded W52 stream. A second inferred chain closes the selected
weighted pattern to `A454:qualified_recovery_stream_ready`. These are semantic
consequences of the measured calibration plus the exact first-encounter
compiler, not duplicated measurements.

## Mechanism read from the graph

The decisive information is not simply “give the strongest individual Reader
more slots.” `proof_best_single` was individually positive in only seven of
eight remaining96 blocks, yet the joint optimum gives it the same two slots as
`hybrid_proof_top16_equal` and gives `proof_borda_top32` one slot. Therefore the
portfolio gain comes from complementary rank errors and slot phase, not from a
weighted average of marginal Reader scores.

The top five schedules all use the same `H=2, O=2, B=1` frequency vector and
share the same best minimum block gain. Phase then selects `BOOHH` by aggregate
gain. This localizes the next high-information frequency direction: extend the
prospectively declared `H:O:B = 2:2:1` ray while keeping exact per-component
proposal bounds, rather than returning to majority consensus or adding new
proof features blindly.

The W52 stream remains close to A451 globally (Spearman
`0.9975947948955896`) but changes the executable prefix materially: top-65,536
overlap is `0.8976287841796875`, and 11,498,921 cells move earlier. Against the
A453 deadline-median stream, Spearman is `0.9603088254247634` and top-65,536
overlap is `0.8478851318359375`. A454 is therefore a calibrated refinement of
the robust portfolio and a materially different prefix from the consensus
compiler.

## Gap and next action

The embedded gap requests an exclusive queued A454 W52 recovery execution.
The scientific order is:

1. bind the committed A454 pair stream to the already qualified complete-cell
   engine after A452;
2. preserve the existing queue and consume no predecessor target outcome;
3. in the foreground, test the newly localized `2:2:1` frequency ray on the
   same strict no-refit calibration before deciding whether a later schedule
   deserves another recovery arm.

This readback makes A454 a retained positive mechanism result and converts the
A453 consensus boundary into a concrete next experiment.

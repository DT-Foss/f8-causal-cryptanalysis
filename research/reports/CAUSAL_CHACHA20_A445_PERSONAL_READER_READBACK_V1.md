# A445 personal authentic-Causal readback

The main agent opened `chacha20_round20_w52_crossfit_raw_shape_reader_transfer_a445_v1.causal` with the authoritative `dotcausal.io.CausalReader` and read the semantic graph, not a JSON or integrity-only surrogate.

## Reader gate

- API id: `a445raw`
- Explicit triplets: 4
- Materialized inferred triplets: 1
- Embedded rules: 4
- Clusters: 1
- Gaps: 1
- Causal SHA-256: `93b3509521998978da0d348f2cc1fc99bfdd0639df6cbf0246d53d0aa79d4323`

## Personally read chain

1. The exact A375 target-normalized 532-channel panel enters an eight-fold, complete-block-exclusive atlas of absolute, signed and squared raw-shape models.
2. The preregistered worst-block-first selector retains `borda_sum_baseline`; its minimum held-block gain is `+0.3287263877670634` bits and all eight blocks remain positive.
3. Every learned raw-shape family is weaker. The two positive all-128 summaries are `abs_fisher_top8` at `+0.04427020340549159` bits and `square_fisher_top32` at `+0.11252696966311326` bits, but neither is positive across all held blocks; the other raw-shape summaries are below zero.
4. Consequently the complete W52 transfer is exactly the already frozen A442 Borda schedule: pair Spearman `1.0`, top-65,536 overlap `1.0`, and pair-stream SHA-256 `b2b6e7a0b716083ae357e1dcde436a68e8ca64334ac5ec9009d74742989c6d48`.
5. The materialized inferred closure connects the original 532-channel panel directly to `A445:raw_shape_representation_boundary`.

## Scientific interpretation

The useful transferable information is not captured by a global linear, diagonal-Gaussian or centroid model of signed or magnitude-valued channels. A442's gain instead survives as relative ordering agreement among diverse Readers. This rules out another magnitude reweighting as the next high-value branch.

The Reader's sole gap is exact and actionable: `typed_hard_contradiction_or_clause_provenance_topology`. The next experiment therefore changes the object being modeled—from per-cell channel vectors to the topology and provenance of contradictions, propagations and learned clauses under candidate assumptions.

# Nightrun Direct-Causal Matrix

This matrix consolidates only Reader-reconstructed artifacts that survived the
current controls.  Scope is stated per row; a full-round known-key cross-round
result remains a full-round result and is not relabeled as reduced-round merely
because it is an internal-boundary measurement.

| line | retained object | strongest evidence | boundary |
|---|---|---|---|
| AES | R3 peeled, nodes 13/11/14/12 | 18--19/20 with pooled variance, shared BvN, unseen bits and new keys; extends to byte XORs | Random→counter transfers; counter→random and mixed-domain training collapse. Not domain-invariant. |
| AES | R1/R3 frontier | R1 20/20, R3 18/20 over R1--R10; fresh R1-vs-R3 20/20 each | R2 and R4--R10 do not form comparable peaks. |
| ChaCha | R3 core, nodes 10/13/5/9 | R3-vs-R20 bit-transfer 13--14/20 vs R20 20/20, shared routes, pooled reader, pre-feed-forward core | Random-base dependent; sequential bases do not retain the top-4 graph. |
| ChaCha | R2 byte atlas, nodes 5/52/11/7; minimal byte 7 for R1/R2 only | R1/R2, R2/R3, R2/R20, bit and byte interventions, Random↔Sequential, and counter-word-masked full output all reach 10/10 per binary class in their stated runs; byte 7 alone is R1/R2 10/10 in both domain directions | Byte 7 alone is R2/R3 5/10, so R2/R3 needs the four-node atlas. This atlas itself carries no full ChaCha20 claim. |
| ChaCha20 | full 20-round feed-forward endpoint Readers | RFC gate; public constants/counter/nonce reconstruct 40,000/40,000 eight-lane core states without key-lane input, and the known-key recipe reconstructs 40,000/40,000 complete 512-bit cores. Both 80-route banks give zero words. | Exact block-function endpoint relation, separate from the reduced-round output atlases. Public-position core lanes are not key words. |
| ChaCha20 carry spectrum | addend-conditioned XOR-peeling representation bridge | 40.96M/40.96M exact carry identities; predicted/observed bit accuracy 0.551271/0.551215 over 512 lane/bit cells with r=0.999953; 62.15 complete words predicted and 60 observed. | Wrong-lane prediction falls to r=0.358; spectrum is tied to actual initial lane values. |
| PRESENT-80 | R3/R6 direct-output binary graph; minimal post-S-box pair `(7,14)` | Full entropy graph transfers both Random↔Counter at 20/20 per class. Collision/byte-XOR/bit-bias controls survive. Exact final-stage views show S-box amplification. Joint pair `(7,14)` is 20/20 per class for Random→Counter bit and byte XOR, Random→Mixed, and Mixed→Random. | Pair `(7,14)` fails R3 for Counter→Random (7/20) and Counter→Mixed (11/20), so it is a directed-domain minimal atlas. Broad R1--R6 is not domain-invariant; late R24--R31 screens are chance-level. |
| PRESENT-128 | full-round known-key R31→R32 F8; exact seven-edge pLayer-fixed-point graph | Predicted transfer from PRESENT-80; all 5 fresh keys exceed all BvN routes with Z 749.68--1390.45. Exact derivation gives 7 nonzero and 4089 zero population cells; empirical top seven match exactly. | Complete for the single-bit `E_r` vs `E_r xor E_(r+1)` population support. Joint higher-order probes are a distinct object. |
| SHA-256 | full 64-step compression feed-forward; eight universal same-lane bit-0 edges | Fixed-IV and random-chaining modes each pass 40,000/40,000 identities; total MI 5.5444/5.5435, Z 3513--15085 | Universal for bit 0 of each of eight lanes. Fixed-IV carry spectrum supplies 12 exact cells total. |
| SHA-512 | full 80-step compression feed-forward; SHA-256 word-width transfer | Fixed-IV and random-chaining modes each pass 40,000/40,000 identities; total MI 5.5442/5.5446, Z 1913--13101 | Universal for bit 0 of each lane. Fixed-IV carry spectrum supplies 11 exact cells total. |
| SHA-2 carry spectrum | exact fixed-addend full-word channel | 3.84M/3.84M conditional carry identities across SHA-256/512; analytic/observed r 0.999597/0.999492 | Higher-bit MI depends on the chaining word; minimal graphs contain only structurally exact no-carry cells. |
| BLAKE3 | full seven-round compression output Reader | Seven official XOF vectors; first 32 bytes give 320,000/320,000 exact final lane-pair XOR words. Complete 64-byte output plus known CV reconstructs 40,000/40,000 complete 512-bit post-round states; all 80 BvN routes give zero words. | The complete-state result requires full compression output plus CV. A standalone 32-byte digest carries the eight pair-XOR words, not separated state halves. |
| BLAKE3 borrow spectrum | exact XOR-to-subtraction representation bridge | Four-state coupled-borrow theorem; 20.48M/20.48M bit identities, analytic/observed accuracy 0.6059028/0.6059255, r=0.999969; `(3/4)^31` predicts 85.72 full words and 87 occur. | Formula-substitution spectrum at the full-compression endpoint; separate from the exact XOR Reader. |
| SHAKE128/256 | full 24-round Keccak-f[1600] first-squeeze rate Readers | Zero-state permutation and FIPS/hashlib gates; 80,000/80,000 rate states, 1.52M lanes and 97.28M bits reconstructed. Complete basis proofs give rank/kernel 1344/256 and 1088/512. All 160 BvN banks and representation controls give zero lanes. | Exact squeeze coordinate projection. One rate block leaves precisely the capacity-coordinate kernel; no one-block capacity reconstruction is asserted. |
| SHAKE capacity Jacobian | capacity-to-next-rate response after another full 24-round permutation | Ten complete Boolean Jacobians: SHAKE128 rank 256 x5, SHAKE256 rank 512 x5; 3,840/3,840 same-base single-bit interventions identified. BvN, cross-base and lane controls give zero labels. | Base-local first-order observability. All 1,280 two-bit tests activate dense nonlinear interaction; no ordinary-output or global-linear inverse claim. |
| SHAKE capacity-window inference | exact consecutive-block reverse query with known capacity complement | SHAKE128/256 windows 8/12/16/20 all uniquely recovered; 2,236,928 complete 24-round candidate evaluations, one two-lane survivor and one complete-rate survivor per run, zero wrong-target candidates. | Work is exactly `2^k`; retained as the executable 20-bit inference baseline, not full-capacity reconstruction. |
| SHAKE bit-sliced capacity-window consistency | candidate-axis-transposed exact 24-round Reader | A 64-state independent implementation gate checks all 102,400 output bits. SHAKE128/256 20- and 24-coordinate windows are all uniquely recovered; 35,651,584 logical candidates are represented by 557,056 packed states, and every independent target has zero survivors. | The representation reduces machine-word state evaluations by exactly 64 while retaining the logical `2^k` search space. The known rate and capacity complement remain explicit inputs. |
| SHAKE native capacity-window consistency | fused C11/POSIX candidate-axis full-round Reader | Native-vs-scalar checks match all 102,400 state bits and native-vs-NumPy candidate masks match exactly for both variants. SHAKE128/256 remain uniquely consistent at 24, 28, and 32 coordinates; the 32-coordinate artifact exhausts 8,589,934,592 logical candidates as 134,217,728 packs with zero independent-target survivors. | Bounded streaming and threads move the executable frontier, while logical work remains `2^k`. Rate and capacity complement are explicit known coordinates; this is not an all-capacity claim. |
| SHAKE Boolean CNF Reader | exact 24-round Tseitin circuit with complete next-rate constraints | SHAKE128 windows 4/8/12 are reconstructed exactly and second-model queries are UNSAT; width 16 reaches the configured 120-second boundary. The exact circuits contain 139,952--144,125 variables and 525,709--541,204 clauses. | Internal decisions already exceed explicit assignment counts at solved widths. The CNF is an exact representation but not an executable scaling improvement over native bit slicing. |
| SHAKE prefix-observability frontier | complete `2^16` per-round truth-space Reader | For SHAKE128/256, fixed rate coordinates with no assigned prefix fall 1344/1088 -> 1060/871 -> 8/10 -> 0/0 over rounds 0/1/2/3. At R24, prefixes through 12/16 retain zero constants; the final 3/2/1-coordinate counts match random-function expectations closely. | Exact output prefixes discriminate complete assignments—32 bits leave one each—but coordinate-wise partial-branch propagation has no early certificate after round 3. |
| SHAKE affine-hull prefix Reader | exact GF(2) hulls of first 128 rate coordinates over every `2^16` branch partition | Prefix 8 leaves all 256 branches; prefix 9 leaves 195/512; prefix 10 leaves exactly the actual branch out of 1,024 in both SHAKE128/256 and remains unique at 11/12. Actual-branch ranks are 128/127/63/31/15 for prefixes 8/9/10/11/12, and false survivors match rank-derived random expectations. | Joint parity conditions retain branch information after all individual coordinates vary. Current construction still materializes the complete truth space; deriving hull constraints without all suffix points is the open representation step. |
| SHAKE algebraic-degree frontier | exact ANFs of the first 128 rate coordinates over complete `2^16` capacity-window spaces | Maximum degree progresses 0/1/3, reaches 7/6 at R3 and 14/12 at R4, then reaches the full 16 in both variants at R5. Mean coefficient counts at R5 are 32,768.09/32,773.56 against random expectation 32,768 and remain saturated through R24. | Truth tables are already balanced at R3 while ANFs remain only 1.34%/1.40% dense. The exploitable sparse algebraic zone ends between R3 and R5; direct full-round ANF expansion is dense. |
| SHAKE Boolean-influence frontier | complete single-window-coordinate interventions against all 1,600 state coordinates over six `2^16` truth spaces | R2 has 8,308--8,927 of 25,600 nonzero cells; R3 has 25,597--25,600 and 1,597--1,600 fully coupled coordinates. R4--R24 have all 25,600 cells nonzero in every trial, with R4/R24 maximum deviations from influence 0.5 of 0.013062/0.012054. | Sparse R3 ANFs are already virtually all-to-all in variable support. R2 is the last measured incomplete influence-support layer, while the monomial graph is already complete there; R4 is the first uniformly all-to-all layer. |
| SHAKE shared-ANF compression | lossless global monomial dictionary plus packed 1,600-coordinate coefficient matrix; generic codecs and ordered double cascades | At R3, raw 13.11MB truth spaces compress only to 12.96/13.07MB, while shared-ANF+LZMA reaches 634/659KB, a 20.44x/19.84x advantage. R2 advantages are 44.65x/44.29x; R4 falls to about 1.02x and R24 to none. All four persisted R2/R3 records reconstruct 419.43M truth values exactly. | The monomial primal graph is already complete at R2; compression comes from reuse of only 275/277 low-degree features, not independent variable components. No two-codec cascade beats single LZMA. |
| SHAKE symbolic R2 compiler | exact Boolean-ring compilation of all 1,600 state formulas without `2^k` truth tables | Full SHAKE128 capacity (256 variables) yields 6,918,733 degree-at-most-4 coefficients; full SHAKE256 capacity (512 variables) yields 64,724,568. The 512-bit coordinate-sparse interface is 4.142GB, a `2^487.696` factor below its formal truth table. Width-16 coefficient hashes match the exhaustive A133 artifacts and 94,720 independent state bits match across all widths. | This changes the R2 representation exponent, not the R3--R24 suffix. The R2 primal graph is complete; the gain is shared symbolic features rather than independent components. |
| SHAKE symbolic-R2 native-XOR Reader | exact R2 Boolean-ring interface joined to an exact R3--R24 Boolean suffix and the complete next-rate observation | Widths 4/8/12 reconstruct exactly and blocked-model queries are UNSAT. Native-XOR first-query decisions are 2,025/8,252/35,088, only 3.53%/17.02%/5.98% of the canonical CNF counts. | The monolithic width-16 instance retains the configured boundary; solved-width work identifies native XOR preservation as a major representation gain, not by itself a complete decomposition. |
| SHAKE symbolic-R2 partition Reader | complete disjoint low-four-coordinate prefix schedule over the 16-coordinate instance | The ground-truth-blind 16-branch schedule returns assignment 35,837 from branch 13 in 5,967 decisions; independent 24-round evaluation matches all 1,344 next-rate bits. | Fifteen branches reach their local limit, so this artifact establishes autonomous model reconstruction while leaving branch-local global uniqueness certificates as the next solver target. Five workers are the retained 10-core/16-GB resource configuration. |
| SHAKE symbolic prefix-split frontier | exact R1/R2/R3 Boolean-ring interfaces attached to the same complete remaining-round systems | At width 12, decisions are 2,986/35,088/4,441 for R1/R2/R3; R1 is 196.46x below canonical CNF. On the fixed A136 width-16 model branch, R1 uses 1,909 decisions versus 5,967 for R2, while R3 reaches the configured boundary after expanding to 7,512 monomials. | R2 remains the compact storage interface, but R1 is the measured Reader optimum because it preserves the second round as local explicit equations. Compression optimum and decision optimum are distinct. |
| SHAKE symbolic-R1 scaling Reader | A137-selected exact R1 interface plus 23 explicit rounds, no fixed prefix | The monolithic 16-coordinate system returns assignment 35,837 in 4,701 decisions, 7.17% of the complete assignment count; independent evaluation matches all 1,344 rate bits. | The blocked query and monolithic widths 20/24 reach the configured boundary. R1 formulas remain small and degree at most two, selecting subspace decomposition over the same interface. |
| SHAKE symbolic-R1 partition topology | Three complete width-20 schedules over the identical A138 R1 SMT: low four, high four, and formula-graph max-cover four | Every schedule contains 16 disjoint branches with 16 free coordinates; all 48 branches return `unknown` at 60 seconds with five one-thread workers. The graph selector covers 14/28 quadratic edges and chooses `[4,9,17,18]` as the first of 14 ties without assignment or target input. | Four fixed coordinates are insufficient here regardless of low/high placement or static max-edge coverage. Free dimension alone does not predict solver work; deeper or suffix-aware decomposition is retained. |
| SHAKE256 symbolic-R1 transfer | A137 SHAKE128 R1 choice instantiated as a hypothesis only, then joined to 23 exact rounds and the complete 1,088-bit SHAKE256 rate | Monolithic widths 16/20/24 all return `unknown` with one thread and a 120-second first-query limit. Interfaces stay degree at most two with 23/35/76 shared monomials. | Direct monolithic transfer of the SHAKE128-selected split is not resolved under these parameters. No SHAKE128 outcome is imported; a SHAKE256-specific split or partition is the next test. |
| SHAKE symbolic-R1 structural-depth frontier | Exact 28-edge R1 quadratic graph; complete graph-only Structural-6 schedule plus one posthoc-conditioned branch at depths 4/6/8/10 | Structural-6 uniquely selects `[4,9,12,15,17,18]`, covers 20/28 edges, and all 64 complete-plan branches return `unknown` at 30 seconds. Under a uniform 60-second posthoc-conditioned frontier, k4/k6 are `unknown`, k8 returns 227,581 in 1,442 decisions, and k10 returns it in 773; both models match all 1,344 rate bits independently. | The measured minimum conditioned depth is k8. Branch values in the depth frontier come from the instrumented assignment, so this row localizes a mechanism threshold rather than performing autonomous search. |
| SHAKE symbolic-R1 Z3 strategy and Structural-6 transfer | Six predeclared processing routes over byte-identical A138 formulas; verified winner transferred without change to width 20 and all 64 Structural-6 branches | Native-XOR `QF_UF` is the only verified width-16 `sat` route, returning 35,837 in 4,701 decisions. Width 20 returns `unknown` after 11,124 decisions; its Structural-6 transfer records 358,361 decisions and 64 `unknown` statuses. | Processing strategy alone does not resolve the width-20 formula or its six-coordinate plan under the tested limits. Route selection excludes assignment and wall time; no candidate is emitted by either width-20 run. |
| SHAKE symbolic-R1 assignment-free k8 Reader | Graph-only maximum-cover selection, complete ascending 256-value plan, five-process waves, and independent full-rate candidate evaluation | Coordinates `[1,2,4,9,10,12,15,18]` cover 24/28 edges. Projections 0--39 execute in eight complete waves; projection 38 returns assignment 227,581 in 1,442 decisions and independently matches all 1,344 rate bits. | Autonomous model finding for the fixed k8 construction. Statuses are 1 `sat` and 39 `unknown`; verified early stop leaves the remaining plan unexecuted, so global uniqueness is not certified. |
| SHAKE symbolic-R1 width-24 minimum-cover Reader | Exact nine-disjoint-edge graph; certified size-9 minimum vertex cover; complete 512-subspace formula schedule; one uniform 120-second budget; independent full-rate model gate | The first four complete five-process waves return one `sat` and 19 `unknown`; projection 319 reconstructs assignment 4,845,375 in 4,734 decisions and 2,380 conflicts, matching all 1,344 rate bits independently. | Assignment-free runtime model finding for the retained width-24 system. Neither assignment nor target projection is a runtime input. The formula-order hypothesis was developed from the same instance and is not a blind holdout; verified early stop leaves 492 values unexecuted and does not certify global uniqueness. |
| SHAKE prospective symbolic-R1 width-24 transfer | Hash-derived unseen capacity window `[143,167)`; publicly frozen graph-to-cover Reader; exact R1 interface; 23 explicit suffix rounds; full 1,344-bit next-rate target | The exact R1 graph has zero quadratic edges, the unique minimum cover is empty, and the complete schedule is one unconditioned `2^24` subspace. Its 120-second query is `unknown`; the independently checked posthoc witness matches all rate bits. | Prospective A151 transfer boundary retained. The new R1 interface is affine, selecting exact basis/pivot and induced R2 interaction analysis instead of a deeper cover. |
| SHAKE symbolic-R1 exact affine basis | Hash-pinned A152 cleared template; all 1,600 affine R1 coordinates; exact `1600 x 24` GF(2) matrix; ascending row basis and inverse | Rank 24, input nullity 0, and 1,576 affine output relations. The first independent basis is systematic: 24 output deltas are a permutation of the 24 inputs. Two-sided inversion and 307,200 three-way state-bit checks pass. | The empty R1 graph is not information loss. It exposes an exact replacement coordinate system without target, solver, or instrumented-assignment input. |
| SHAKE symbolic-R2 pivot-basis interaction Reader | Exact A154 inverse substituted into all 1,600 R2 ANFs; complete degree-two dictionary and graph proof | Both original and pivot bases contain all 276 quadratic pairs, so the graph is exactly K24. Its minimum vertex cover is 23 and leaves one free coordinate; 307,200 original/pivot/concrete state bits match. | Pairwise interaction saturation occurs exactly from R1 to R2 and is basis invariant here. R2 cover partitioning is structurally exhausted; systematic R1 encoder reduction is selected. |
| SHAKE systematic-R1 full-round encoder frontier | Four frozen original/pivot-order encoders with 27 multi-term R1 rows defined or inlined; unchanged R2--R24 suffix and all 1,344 target bits | Removes 1,573 or 1,600 prefix definitions and up to 101,268 bytes. All four sequential 120-second Z3 4.15.4 runs return `unknown`, code zero, no external timeout, and no assignment; decisions span 13,428--15,482. | Exact alias/constant elimination maps a new monolithic resource boundary. It selects direct shared-R2 monomial compilation, not another R1 ordering or K24 vertex cover. |
| SHAKE shared-R2 full-round encoder frontier | Exact 301-monomial R2 dictionary with 276 quadratic terms defined once; original/pivot input order crossed with first-use/frequency definition order; R3--R24 and all target bits unchanged | Removes one generic suffix round and two R2 aliases. All four runs are `unknown`; original order reaches 20,649/20,703 decisions, pivot order 11,853/12,284. Smallest formula is 184,524 bytes below A156. | Input permutation, not monomial declaration order, is the dominant measured variable. The exact weighted R2 occurrence matrix selects the next assignment-free ordering frontier. |
| SHAKE weighted-R2 input-order frontier | Exact K24 occurrence matrix; static weighted-degree high/low and dynamic remaining-weight max/min orders; identical frequency-ordered shared-R2 suffix relation | All four are `unknown`; decision counters span 10,990, 18,485, 17,799 and 23,097 with no error, external timeout, model or order-time assignment input. | Large traversal separation is retained, but no direction is promoted under time-bounded unknown outcomes. Fixed Z3 resource units are selected before further ordering claims. |
| SHAKE fixed-resource weighted-R2 order frontier | Byte-exact replay of all four A158 formulas; identical 500,000,000-unit Z3 `rlimit`; sequential one-thread execution; volatile time/memory excluded | All four exhaust the cap as `unknown`. Decisions are 6,940, 14,386, 13,298 and 18,936 in A158 order; the descending/greedy-max/ascending/greedy-min rank is unchanged. | The traversal split is deterministic rather than a wall-clock artifact, but no model is emitted. The next exact variable is a target- and assignment-free affine gauge minimizing R2 linear incidence while preserving K24. |
| SHAKE exact R2 affine-gauge Reader | All `2^24` shifts `x=y XOR s`; exact 38,400-term signed spectrum and integer FWHT; complete-domain optimum and Parseval certificate | Unique minimum `0x8e26db` reduces linear incidences 8,698 to 8,413; constants 831 to 823; all 15,972 per-coordinate quadratic incidences and K24 remain fixed. Sixty-four three-way assignments match 307,200 state bits. | Supplies a globally optimal assignment-free polarity gauge. Cross it with the four frozen A158 orders under A159's fixed `rlimit`, with exact affine/permutation model recovery. |
| SHAKE affine-gauge x weighted-order fixed-resource frontier | A160 gauge crossed with all four A158 orders; exact same A159 `rlimit`; unchanged 240,707-line suffix/target tail; inverse permutation plus XOR model recovery | All four are `unknown`. Decision deltas versus A159 are +4,919, -3,893, -7,474 and -11,399; the rank changes to greedy-max, greedy-min, ascending, descending. | Total linear-incidence minimization is not uniformly favorable. The retained mechanism is a deterministic gauge-by-order interaction, selecting complete-domain order-weighted Walsh objectives. |
| SHAKE order-weighted affine-gauge Reader | Four frozen orders x canonical front/back positional weights; eight complete `2^24` integer FWHT landscapes; four unique selected gauges | Every optimum is unique. Selected masks are `0x498a92`, `0x4e1e28`, `0x8c161b`, `0x954b3c`; each improves zero and A160 under its own objective. All four polynomial interfaces retain 15,972 quadratic incidences and pass 1,228,800 aggregate semantic state-bit checks. | Produces an eight-pair target/assignment/solver-free factorial plan. Execute all pairs under the A159 budget rather than selecting from A161's observed ranking. |
| SHAKE order-weighted gauge x order fixed-resource frontier | All eight A162 order/gauge cells; exact affine/permutation recovery; same 500,000,000-unit cap; A159 zero and A161 unweighted-gauge controls | All eight are `unknown`. Seven reduce decisions against zero; two reduce them against A160. Front loading wins descending/ascending/greedy-min, back loading wins greedy-max; decisions span 6,785--13,930. | Retains a strong factorial gauge/order interaction while showing that positional linear incidence alone is insufficient. The next prospective objective must add a concrete structural variable rather than another arbitrary weight curve. |
| SHAKE complete four-gauge x four-order factorial | Frozen 16-cell Cartesian product; exactly eight nonrepeating A164 cells joined to eight A163 cells; identical fixed resource cap; exact rational two-factor decomposition | Gauge `0x4e1e28` wins all four orders with mean 5,779 decisions. Weighted-degree ascending reaches a new 4,402 minimum, 24.4% below the previous best. Gauge main effect is -3,079.375 and maximum absolute interaction is 3,095.625 decisions. | Isolates a transferable gauge main effect plus comparably large order coupling. The next algebraic Reader targets the concrete constant/alias/elimination/suffix-cone structure of `0x4e1e28`. |
| SHAKE prospective A152 native full-round reconstruction | A152's public cleared template, 1,344-bit target and ordered 24-bit window; retained executable recipe read from native Causal artifact; complete `2^24` domain in 262,144 64-candidate packs; full bit-flipped-target control | Unique assignment 9,279,571 survives the 128-bit filter and independent 1,344-bit 24-round check; the control has zero matches. A152 posthoc comparison occurs only after execution and matches exactly. Fresh end-to-end M4 Mac mini time: 1.61 seconds. | Converts the prospective symbolic resource boundary into exact assignment-free model reconstruction on consumer hardware and cleanly separates solver representation from executable domain size. |
| SHAKE signed-alias compiler intervention | A164's four frozen orders and universal gauge; exact 23-depth suffix-cone analysis; semantics-preserving normalization of the sole materialized negative R2 alias; identical fixed resource cap | Static incidence selects `0x498a92` at every depth, not `0x4e1e28`. Alias normalization changes decisions by +2,008, -977, -1,623 and -1,182; ascending reaches a new 3,425 minimum. | Refutes static suffix incidence as the universal-gauge mechanism and retains an exact order-dependent connected-alias effect. |
| SHAKE ID-preserving and normalized-RHS alias controls | A167 preserves every downstream declaration ID with disconnected `s1215`; A168 preserves the connected node and rewrites only `xor(true,x)` to `not(x)` | A167 decisions exactly equal A166 in all four orders, so the ID-shift decision effect is zero. A168 exactly reproduces all four materialized-control decision counts, so RHS spelling has zero decision effect. | Isolates removal of the connected negative-alias node from declaration numbering and equivalent RHS syntax. |
| SHAKE two-consumer alias-fanout decomposition | Column-only and theta-only fanout-one arms joined to A166 fanout zero and A168 fanout two; exact per-order Boolean-lattice Mobius coefficients | Column and theta main-effect L1 magnitudes are 4,247 and 4,222 decisions; interaction L1 is 3,289 and every order has nonzero interaction. | Neither consumer alone is sufficient. The retained mechanism is a joint two-consumer solver-graph effect conditioned on input order. |
| SHAKE reversed-order alias polarity | Exact reversal of all four 24-coordinate orders; paired inline/materialized arms; unchanged relation, gauge and fixed resource cap | Reversed effects are -1,281, -2,898, -942 and +1,018: two signs flip and two persist. A matched weighted subset exposes an adjacent `0/12` swap. | Complete order reversal is not a polarity law; the adjacent swap becomes the next prospective intervention. |
| SHAKE central alias-boundary transfers | A172 Greedy-Max `0/12` prospective transfer; A173 position-matched Weighted-Descending contrast; A174 prospective partner replacement `0` to `22` at positions 11/12 | Directional deltas are +456, +10,955 and +2,196. A172 reverses its frozen prediction; A173 preserves the positive direction across order family; A174 confirms it across the second tested partner. | Establishes an exact compiler x adjacent-order boundary with explicit order-family magnitude and a two-partner prospective transfer. No solver model is emitted. |
| SHAKE alpha-renamed central boundary | Every declared SMT-symbol suffix incremented bijectively; inverse restores all four A174 formulas byte-for-byte; graph, orders, semantics and resource cap unchanged | Three decision counts reproduce exactly, the fourth changes by +398, and every canonical observation hash changes. The directional delta remains positive and increases from +2,196 to +2,594. | Exact observation invariance is name-sensitive, while the tested central-boundary direction is alpha-robust. A171 is intentionally unused. |
| SHAKE input-declaration-order boundary | Only the `x11`/`x12` declaration lines are swapped at indices 11/12; assertions, symbols, graph, `get-value` order, byte count and resource cap remain fixed; second swap is an exact inverse | All four decision counts, every retained solver counter and all canonical observation hashes reproduce A174 exactly; the directional delta remains +2,196. | Establishes exact local input-declaration-order invariance for the tested parser/solver scope without emitting a model. |
| SHAKE256 prospective native width-32 reconstruction | A176-derived public seed and positions 184--215; retained Causal Reader; complete `2^32` domain as 67,108,864 native packs; 128-bit filter and independent 1,088-bit confirmation | The unique assignment is 2,761,171,082; all rate bits match independently and the bit-flipped control has zero filter and full matches. No pack is resumed and execution does not stop early. | Extends exact assignment-free native reconstruction from width 28 to the complete width-32 domain under a prospectively frozen relation. |
| ChaCha20 prospective fullround 32-bit partial-key recovery | Standard block function; key word 0 hidden; other 224 key bits, counter, nonce and complete block output known; complete `2^32` C11 candidate-axis execution | The unique 64-bit filter and independent 512-bit match is `0x903db747`; the bit-flipped control returns no candidate. The hidden word first appears in the post-execution record. | Exact partial-key recovery for the declared standard ChaCha20 scope, separated from the earlier known-key endpoint Readers. |
| ChaCha20 vector-256 complete-domain replay | Prospectively frozen four-sublane representation; complete known A178 `2^32` challenge; scalar, boundary-mask and direct v1/v2 equivalence gates | The same unique `0x903db747` match and empty control are retained while native states fall from 67,108,864 uint64 packs to 16,777,216 vector states. | Exact semantic equivalence and fourfold packing advance; the known A178 answer is used only after complete execution. |
| ChaCha20 Apple M4 Metal complete-domain replay | Persistent Swift/Metal host; warnings-as-errors build; 256-block cross gate; three boundary filters; sixteen complete GPU batches | All `2^32` candidates execute, reproduce `0x903db747`, reject the control and independently confirm 512 bits. | Exact Metal equivalence and execution acceleration for the known A178/A179 challenge; local timing is noncanonical context. |
| ChaCha20 prospective fullround 36-bit partial-key recovery | Fresh frozen target; word 0 plus word-1 low nibble hidden; 220 key bits known; 16 complete Metal slices and 256 batches | The unique assignment is `0x3069630b3`; the control is empty and all 512 bits confirm independently after complete `2^36` execution. | Exact recovery for the declared 36-bit partial-key scope. |
| ChaCha20 prospective fullround 38-bit partial-key recovery | Fresh frozen target; word 0 plus word-1 low six bits hidden; 218 key bits known; 64 complete Metal slices and 1,024 batches | The unique assignment is `0x1a15b63e04`; the control is empty and all 512 bits confirm independently after complete `2^38` execution. | Exact recovery for the declared 38-bit partial-key scope. |
| ChaCha20 prospective fullround 40-bit partial-key recovery | Fresh frozen target; word 0 plus word-1 low byte hidden; 216 key bits known; 256 complete Metal slices and 4,096 batches | The unique assignment is `0x2874913214`; the control is empty and all 512 bits confirm independently after complete `2^40` execution. | Exact recovery for the declared 40-bit partial-key scope. |
| FEAL-32X | full-round R30→R32 distance-2 Reader inverse | NTT official ciphertext/intermediate/subkey gates; Reader executes a five-edge recipe and reconstructs 40,000/40,000 complete 32-bit words over five fresh keys. All BvN and previous-round-subkey controls give zero complete words. | Raw 1-bit MI is deliberately masked (Z -1.05 to -0.02); the retained object is the exact joint 32-bit relation. |
| SHACAL-2 | full-round R63→R64 shared-T1 cancellation Reader | Two NESSIE KATs; 40,000/40,000 d63 words and 1.28M/1.28M bits reconstructed from the three-edge Reader recipe. All BvN and wrong-Sigma controls give zero words and about 50% bits. | Formula-aligned internal boundary; mechanism is modular cancellation of the shared T1 accumulator, not SHA-2 feed-forward. |
| SPARKLE-256/384/512 | complete-permutation final full-step projection and finite-order linear Reader | Hash-pinned official C gates; 120,000/120,000 left-half states reconstructed from only final right halves, plus 120,000/120,000 full pre-final-step states via `L^(order-1)`. Complete-basis minimal orders are 6/30/12; aggregate quotient orders 3/10/3. All 240 BvN routes give zero half-state and zero 64-bit-branch matches. | Exact internal full-step family relation. The copied projection is the generalized-Feistel route after Alzette; the stated scope is not an external-secret result. |
| Threefish-256 | direct late-round output codec | R64--R72 is 17/180, below chance | Closed; matches local-transition explanation of legacy F8. |
| ML-KEM | native INDCPA noise→ciphertext | 50,000/50,000 gates, 3/20 four-class accuracy | Closed current chunk-entropy codec. |
| ML-KEM | native INDCPA keyseed→public key | two independent 50,000/50,000-gate blocks, both chance | Closed current chunk-entropy codec. |

## Method controls now mandatory

1. One BvN repair-route bank per exact seed/input-pair batch, shared across
   all compared labels. Class-dependent route seeds were corrected and older
   traversal-R3 interpretation was withdrawn.
2. The `.causal` file is re-opened with `CryptoCausalReader` before every
   reverse query. Writer-side model arrays are not used for held-out scoring.
3. Pooled reader variance is required whenever a class-specific variance floor
   could dominate a small training split.
4. Every new positive needs fresh keys plus a disjoint intervention family;
   where a domain is changed, both directions must be tested before describing
   transfer as symmetric.
5. Functional gates are mandatory for native PQC traces; output-only order
   statistics are not promoted to internal-state claims.

## Next research directions

- For the ChaCha byte atlas, localize the four byte nodes to quarter-round
  paths with exact state-level ablations rather than adding more output
  classifiers.
- For AES R3, test whether a representation that conditions out plaintext
  base-domain scale can recover bidirectional domain transfer; do not relax
  the domain-boundary result without a new held-out test.
- For PRESENT, use the exact pLayer/S-box criterion to predict new SPN transfers
  statically.  Keep the separate direct-output R3/R6 atlas and the full-round
  cross-round F8 theorem as distinct estimands.
- For SPARKLE, use the now-proved branch projection and 6/30/12 linear orders
  as algebraic primitives for a prediction-first two-step composition; do not
  relabel the one-step generalized-Feistel route as a whole-permutation secret
  inference statement.
- Keep PQC on genuinely new native deterministic relations; the current
  noise-coin and keyseed chunk-entropy branches are closed.
- For SHAKE, treat 32-coordinate native consistency as the new executable
  baseline.  The next useful step must change the exponent or expose a new
  relation; simply extending the same exhaustive window is no longer a distinct
  mechanism result.
- The exact prefix frontier rules out single-coordinate constancy as that
  exponent change.  Continue with joint affine/parity constraints or a genuine
  round split, using the transition from incomplete R2 coordinate support to
  nearly all-to-all R3 influence as the localization boundary.
- The affine hull now supplies the joint-relation transition: 128-coordinate
  hull membership identifies the actual 10-bit prefix.  Next derive or predict
  those 65 branch relations without complete suffix enumeration.
- The ANF frontier localizes the remaining algebraic structure: preserve the
  sparse R3 polynomials only as low-degree equations, not as independent
  variable components.  The shared-dictionary result identifies R2 as the
  compact round-split interface: its primal graph is complete, but all 1,600
  formulas reuse only 275/277 monomials.  Keep R3--R24 separate because full
  symbolic substitution reaches all-to-all influence at R4 and random-like ANF
  density at R5.
- The symbolic compiler now provides that R2 interface directly through the
  complete 256/512-bit capacities without an assignment table.  Connect its
  shared features to a separately encoded R3--R24 suffix; do not expand the
  full composition back into ANF.
- The split frontier selected R1 for solver work; graph-only Readers now find
  the canonical width-20 and width-24 models.  At width 24 the successful
  conditioned depth equals an exact minimum vertex cover, and a complete
  formula-ranked uniform-budget plan converts it into runtime-autonomous model
  finding while retaining its same-instance, non-blind design scope.
  A152's publicly frozen new-seed transfer then produces an edgeless R1 graph
  and a unique empty cover, localizing the transfer failure to a fully affine
  R1 interface.  A154 proves that interface is a systematic rank-24 embedding
  with zero input nullity; A155 proves that the next round is already K24 with
  minimum-cover size 23 in the exact pivot basis.  A156 removes every redundant
  R1 prefix definition in four exact encoders but retains the uniform
  monolithic boundary.  A157 directly compiles the 301 shared R2 monomials and
  removes a full generic suffix-round block.  A158 derives four weighted input
  orders and retains a 10,990--23,097 decision separation.  A159 then freezes
  those exact formula bytes under one 500,000,000-unit Z3 `rlimit`; the same
  rank order persists at 6,940--18,936 decisions while every query exhausts the
  cap as `unknown`.  This removes wall-clock scheduling as the explanation and
  selects exhaustive affine-gauge normalization of the R2 linear terms rather
  than another permutation, projection, or K24 cover.  A160 evaluates that
  complete `2^24` domain exactly and finds the unique `0x8e26db` gauge, removing
  285 linear incidences while preserving all 15,972 quadratic incidences.
  A161 crosses this certified gauge with A158's four frozen orders under A159's
  fixed resource protocol.  Three decision counters fall by 27.1--60.2%, the
  former minimum rises 70.9%, and the rank order changes while all outcomes
  remain `unknown`.  This selects order-weighted affine-gauge Walsh landscapes,
  not total term count alone, as the next exact structural Reader.  A162
  computes eight complete landscapes and four unique semantic gauges.  A163
  executes all eight predeclared order/direction pairs under the same fixed
  resource budget.  Seven beat the corresponding zero gauge, but only two beat
  A160 and the preferred direction changes for greedy-max.  A164 then executes
  exactly the eight missing Cartesian-product cells.  `0x4e1e28` wins every
  order and the ascending cell reaches a new 4,402-decision minimum, 24.4%
  below the prior best.  Exact decomposition retains both a -3,079.375 gauge
  main effect and interactions up to 3,095.625 decisions, selecting the
  concrete constant/alias/elimination/suffix-cone structure of this gauge.
  A165 then transfers the retained native Causal Reader to A152's same public
  relation, executes the complete `2^24` domain, and reconstructs the unique
  model 9,279,571 with an independent 1,344-bit match.  The full control target
  has zero matches and the fresh end-to-end run completes in 1.61 seconds on an
  M4 Mac mini.  A152's symbolic boundary is therefore representation-specific,
  while its declared logical domain is directly executable.  A166--A170 then
  isolate the connected negative-alias node, its two suffix consumers and its
  order-conditioned polarity; declaration-ID shifts and equivalent RHS syntax
  have zero decision effect in the matched controls.  A172--A174 localize the
  surviving relation to a central alias-position boundary, retain its positive
  direction across a position-matched order-family contrast and confirm it for
  a second partner.  A175's exact bijective alpha-renaming changes one solver
  trajectory but preserves that direction.  A176 then swaps only the local
  `x11`/`x12` declaration lines and reproduces A174 bit-for-bit, closing that
  parser-order variable.  A177 executes a newly frozen SHAKE256 width-32
  relation over all `2^32` candidates and returns one independently confirmed
  model.  A178 transfers the same complete-domain native pattern to a standard
  ChaCha20 block and recovers one prospectively hidden 32-bit key word exactly.
  A179 establishes vector-256 complete-domain equivalence with an exact fourfold
  native-state reduction.  A181 transfers the full domain to Metal, and
  A182--A184 recover fresh 36-, 38-, and 40-bit partial-key assignments over
  complete domains with independent 512-bit confirmations and empty controls.
  A171 and A180 are intentionally unused.

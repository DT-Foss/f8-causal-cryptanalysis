# A451 authentic Causal Reader — personal semantic readback

## Reader invocation and immutable anchors

The main research agent opened the authentic AI-native artifact directly with
`dotcausal.io.CausalReader(..., verify_integrity=True)` and read the complete
explicit and inferred semantic chain, the embedded rules, the convergence
cluster, and the declared gap.

- Result SHA-256: `f2501e5e85f6d37305473738bb0840c12651720e6f7e3fbab2fc4a253b40bdf6`
- Causal SHA-256: `07d34f4edbdb7d23d97d6b15fb66c21814c1315e3e2ef225f1d57b9e22bf2895`
- Pair-stream SHA-256: `826d10e8cfb8ba2cb51e2d1cee35d29f29b9a313928dbdabbf6b92ad2a546cf9`
- Reader API id: `a451mix`
- Semantic content: 3 explicit triplets, 2 retained inferred triplets, 2
  embedded rules, 1 convergence cluster, and 1 actionable gap.

## Personally read semantic chain

1. `A449:three_complementary_proof_operators`
   --`fixed_slot_round_robin_first_encounter_deduplication`-->
   `A451:complete_nonfactorized_pair_permutation`.
2. `A451:complete_nonfactorized_pair_permutation`
   --`exact_all_pair_rank_inequality`-->
   `A451:exact_threefold_rank_guarantee`.
3. `A451:exact_threefold_rank_guarantee`
   --`memory_mapped_uint16be_pair_readout`-->
   `A451:nonfactorized_W52_recovery_stream_ready`.
4. The first retained closure connects the three A449 readers directly to the
   exact all-cell guarantee; the second connects the complete permutation
   directly to recovery readiness. These are semantically justified closures,
   not fuzzy-name or co-occurrence edges.

## Research information extracted from the graph

The convergence hub is the exact rank inequality, not any particular reader.
This changes the deployment object from an axis-factorized heuristic into a
general random-access pair permutation with a deterministic portfolio regret
bound. The complete artifact contains all 16,777,216 pairs exactly once. The
bound has zero violations; the observed maximum ratio is 2.013698630, the
99th percentile is 1.402589987, and the median is 1.095892959.

The graph also exposes that the fusion is genuinely multi-reader in its early
region. Among the first 65,536 fused cells, 35,422 are first contributed by
`proof_borda_top32`, 14,834 by `hybrid_proof_top16_equal`, and 15,280 by
`proof_best_single`. Against the A442 Borda baseline, the top-65,536 overlap is
0.7763671875, so 14,656 cells are displaced while the exact bounded-regret
property is retained.

## Next experiment selected from the Reader gap

The sole gap is
`qualified_W52_nonfactorized_portfolio_recovery_execution`. The next experiment
is therefore A452: bind the immutable 64-MiB A451 stream to the already
A434-qualified complete-`2^28`-subcell engine through a read-only memory map,
retain the eight disjoint modulo-worker cover and all existing confirmation and
control rules, and queue it exclusively after A450. No prior recovery outcome
or progress is an input to the A452 schedule.

# A446 personal authentic-Causal readback

The main agent opened `chacha20_round20_w52_assumption_cut_clause_provenance_reader_a446_v1.causal` with the authoritative `dotcausal.io.CausalReader` and read the decoded semantic graph, its embedded inference rules, cluster and open gap. This is the AI-native graph readback, not a JSON surrogate or an integrity-only check.

## Reader gate

- API id: `a446cut`
- Explicit triplets: 4
- Materialized inferred triplets: 1
- Embedded rules: 4
- Clusters: 1
- Gaps: 1
- Causal SHA-256: `5ce958b43165d374a52b973ac48ba47edab07c5e8cbc5948e9d6dd132ab89636`

## Personally read chain

1. The complete A359/A363/A367 clause-identity corpus is transformed into a candidate-relative assumption-cut hypergraph. Its 210 typed features retain assumption agreement/opposition, boundary coupling, clause overlap, complementary literals, single-complement resolution, subsumption, connected components and temporal recurrence across horizons 1, 2, 4 and 8. Raw candidate values are absent.
2. Eight complete held blocks select among thirteen frozen pure and hybrid rank operators. The retained operator is `borda_sum_baseline`: minimum held-block gain `+0.3287263877670634` bits, balanced two-corpus gain `+0.5840543224346755` bits, all-128 gain `+0.5963104313759127` bits, and eight of eight positive blocks.
3. Assumption-cut provenance is nevertheless informative as a secondary Reader. Every baseline-heavy hybrid remains positive on all eight held blocks: `hybrid_borda_top8_base2` is strongest at minimum `+0.2257592677`, balanced `+0.376967891`, and all-128 `+0.454850965` bits. Pure provenance does not retain blockwise transfer; its strongest all-128 summary is `provenance_borda_top8` at `+0.037669321` bits with only three positive blocks.
4. Because no nonbaseline operator reaches the Borda minimum, the target-blind W52 schedule remains exactly A442: pair Spearman `1.0`, top-65,536 overlap `1.0`, and pair-stream SHA-256 `b2b6e7a0b716083ae357e1dcde436a68e8ca64334ac5ec9009d74742989c6d48`. W52 target labels, model refits, new solver stages and candidate executions are all zero.
5. The materialized inferred edge closes the original clause-identity corpus directly onto `A446:assumption_cut_provenance_boundary`.

## Scientific interpretation

A446 recovers real candidate-relative structure that A251--A266 had projected away, but the structure becomes transferable only when anchored by the broad A442 rank consensus. Clause-output topology alone therefore does not contain the missing direction. The decisive information must occur before learned-clause identity is flattened: in the implication antecedents, conflict cut, propagation depth or proof-producing contradiction path that generated each clause.

The Reader's sole gap is exact: `proof_producing_conflict_analysis_or_clause_antecedent_instrumentation`. The next experiment changes the observation boundary from learned-clause output to its causal derivation graph; another score reweighting is excluded.
